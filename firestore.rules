rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Solo utenti autenticati con Firebase possono leggere/scrivere
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
